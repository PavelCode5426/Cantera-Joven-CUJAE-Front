version: '3.3'
services:
  frontend:
    container_name: sistema-formacion-jovenes-fronend
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      VITE_URL_SERVER: 'http://152.206.177.242:8050/api/v1'
      BASE_URL: 'http://localhost:3000'
    ports:
      - 8000:3000
    command: npm run deploy